/**
 * Stub migration — this migration was already applied to the database
 * under a previous filename. This stub exists to keep the knex migration
 * history consistent. It is intentionally a no-op.
 */
exports.up = async function (knex) {
  // No-op: schema was already applied
};

exports.down = async function (knex) {
  // No-op
};
